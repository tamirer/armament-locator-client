import {Byte} from "@angular/compiler/src/util";

export class TailArmamentsImages {
  constructor( public id: number, public beforePreflightImage: any, public afterPreflightImage: any,
               public tailId: number, public isHistory: boolean) {
  }
}
